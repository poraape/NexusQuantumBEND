// services/apiClient.ts

// [ARQUIVO DESCONTINUADO]
// Este cliente de API foi descontinuado como parte da refatoração para uma
// arquitetura "frontend-only". Toda a lógica de processamento de dados e
// comunicação com a IA agora é executada diretamente no cliente.
//
// A funcionalidade de orquestração foi movida para o hook:
// -> `src/hooks/useAgentOrchestrator.ts`
//
// Este arquivo é mantido temporariamente para evitar quebras de importação
// durante a transição, mas não possui mais funcionalidade e pode ser removido
// em limpezas futuras do código.

export const apiClient = null;
